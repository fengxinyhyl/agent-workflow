# mock 模式 decision 脚本。
# 单向链路:
#   understand_requirements -> review_breakdown -> give_advice -> done

decision_script:
  understand_requirements:
    - done
  review_breakdown:
    - done
  give_advice:
    - done
