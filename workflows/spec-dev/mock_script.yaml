# Mock decision 脚本。
# 默认走完整主链，强制完成两轮 plan 与两轮 output 循环。
# 同一 task 被多个 state 复用，按访问次数顺序返回 decision。

decision_script:
  planning:
    - done
  plan_review:
    - done   # plan_review_r1
    - done   # plan_review_r2
  plan_refinement:
    - done   # plan_refinement_r1
    - done   # plan_refinement_r2
  execution:
    - done
  output_review:
    - done   # output_review_r1
    - done   # output_review_r2
  output_refinement:
    - done   # output_refinement_r1
    - done   # output_refinement_r2
  validation:
    - approve
  retrospective:
    - done
